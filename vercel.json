{
  "buildCommand": "npm ci && npm run build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "apps/web/dist/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
